<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="JulyWorkoutTheme" parent="AppTheme" >
        <item name="android:colorBackground">@color/color_white</item>
        <item name="android:colorForeground">@color/color_black</item>
    </style>

    <style name="ListItemRecordStyle">
        <item name="android:layout_marginLeft">@dimen/layout_margin_4dp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:drawablePadding">@dimen/padding_4dp</item>
        <item name="android:gravity">center_vertical</item>
    </style>

    <style name="ListItemTextViewStyle">
        <item name="android:layout_marginLeft">@dimen/layout_margin_4dp</item>
        <item name="android:layout_marginRight">@dimen/layout_margin_4dp</item>
        <item name="android:ellipsize">end</item>
        <item name="android:paddingLeft">@dimen/padding_4dp</item>
        <item name="android:paddingRight">@dimen/padding_4dp</item>
        <item name="android:layout_alignParentLeft">true</item>
    </style>
</resources>
